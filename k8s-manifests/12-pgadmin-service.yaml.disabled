# PgAdmin Service (DISABLED - Rename to .yaml to enable)
apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  namespace: pgadmin-stack
spec:
  selector:
    app: pgadmin
  type: NodePort
  ports:
  - port: 80
    targetPort: 80